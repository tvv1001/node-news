/**
 * Minimal mergeFinraSec shim — returns null/empty merged structures.
 */

export function mergedIndividual(crd) {
	return null;
}

export function mergedFirm(id) {
	return null;
}

export default { mergedIndividual, mergedFirm };
